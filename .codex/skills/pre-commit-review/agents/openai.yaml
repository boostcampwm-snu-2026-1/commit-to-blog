interface:
  display_name: "Pre-Commit Review"
  short_description: "Review code before commit in this repo"
  default_prompt: "Use $pre-commit-review to review the current changes against CODE_CONVENTION.md, score them, improve the code if needed, and then continue to the commit decision flow."
