{
  "name": "commit-to-blog",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/server",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @commit-to-blog/server\" \"npm run dev --workspace @commit-to-blog/web\"",
    "build": "npm run build --workspace @commit-to-blog/server && npm run build --workspace @commit-to-blog/web",
    "check": "npm run check --workspace @commit-to-blog/server && npm run check --workspace @commit-to-blog/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}

